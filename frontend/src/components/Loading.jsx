/**
 * Loading Component
 * Displays a spinner during loading states
 */

const Loading = () => {
  return (
    <div className="loading">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
